import { useEffect, useState } from "react";
import {getStoredCart} from '../'

const useCart = products =>{
    const [cart, setCart] = useState([]);

    // useEffect(()=>{

    //     is(products.length){
    //         const savedCart = getStoredCart()
    //     }
    // })
}